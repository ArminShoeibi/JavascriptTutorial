
    <script>

        var name;
        console.log(name); // undefined

        let name2 = "Armin";
        console.log(name2);

        /* You can use $(Dollar), _(Underscore), Alphabets for first character of variables.
         * A variable cannot start with a number.
         * A variable cannot be a reserved keyword.
         */

        let _firstName = "Armin";
        let _lastName = "Shoeibi";
        let $age = 19;


        // declaring 3 variables with just one let keyword
        let numberOne = 1, numberTwo = 2, numberThree = 3;




        // value of a constants cannot change
        const interestRate = 0.3;
        console.log(interestRate);


    </script>